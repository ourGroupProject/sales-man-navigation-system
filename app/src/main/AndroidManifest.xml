<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nilogithan.delboy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="eleven to 11"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".BodyActivity" />
        <activity
            android:name=".bodyActivity2"
            android:label="eleven to 11"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Pending_Task" />
        <activity android:name=".Finished_Task"></activity>
    </application>

</manifest>